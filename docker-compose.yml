services:
  postgres:
    image: postgres:16-alpine
    container_name: churchhub-postgres
    restart: unless-stopped
    environment:
      POSTGRES_USER: churchhub
      POSTGRES_PASSWORD: churchhub
      POSTGRES_DB: churchhub
    ports:
      - "5432:5432"
    volumes:
      - postgres_data:/var/lib/postgresql/data
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U churchhub -d churchhub"]
      interval: 5s
      timeout: 5s
      retries: 5

  redis:
    image: redis:7-alpine
    container_name: churchhub-redis
    restart: unless-stopped
    ports:
      - "6379:6379"
    volumes:
      - redis_data:/data
    healthcheck:
      test: ["CMD", "redis-cli", "ping"]
      interval: 5s
      timeout: 3s
      retries: 5

  minio:
    image: minio/minio:latest
    container_name: churchhub-minio
    restart: unless-stopped
    command: server /data --console-address ":9001"
    environment:
      MINIO_ROOT_USER: churchhub
      MINIO_ROOT_PASSWORD: churchhub-secret
    ports:
      - "9000:9000"
      - "9001:9001"
    volumes:
      - minio_data:/data

  minio-init:
    image: minio/mc:latest
    depends_on:
      minio:
        condition: service_started
    entrypoint: >
      /bin/sh -c "
      mc alias set local http://minio:9000 churchhub churchhub-secret;
      mc mb local/churchhub-uploads --ignore-existing;
      mc anonymous set download local/churchhub-uploads;
      exit 0;
      "

volumes:
  postgres_data:
  redis_data:
  minio_data:
